<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.springboot.wearwave.mapper.SizeMapper">

<select id="sizeList" parameterType="String" resultType="String">
	select item_size from item_size where item_code = #{item_code} 
</select>

</mapper>
